import React from "react";
import App from "../App";
import { mount } from "enzyme";

it("can load data from external service and update the state for coordinates and place data ", () => {});

// Call to load external data
// Expect - update to coordinates
// Expect - update to location data
// Check the travel
